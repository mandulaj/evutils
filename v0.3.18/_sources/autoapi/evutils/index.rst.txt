:py:mod:`evutils`
=================

.. py:module:: evutils

.. autoapi-nested-parse::

   evutils: utilities for event-based data.

   Event-based data processing and visualization utilities.

   To get started::

       import evutils
       ...

   .. rubric:: Notes

   Anything worth calling out up front.



Submodules
----------

.. toctree::
   :hidden:
   :maxdepth: 1

   /autoapi/evutils/chunking/index
   /autoapi/evutils/dataset/index
   /autoapi/evutils/dense/index
   /autoapi/evutils/filtering/index
   /autoapi/evutils/io/index
   /autoapi/evutils/jit/index
   /autoapi/evutils/random/index
   /autoapi/evutils/torch/index
   /autoapi/evutils/transforms/index
   /autoapi/evutils/types/index
   /autoapi/evutils/vis/index

.. list-table::
   :widths: 30 70
   :header-rows: 0

   * - :py:mod:`chunking <evutils.chunking>`
     - Splitting event streams into chunks.
   * - :py:mod:`dataset <evutils.dataset>`
     - Dataset loading and management.
   * - :py:mod:`dense <evutils.dense>`
     - Dense representations of event data.
   * - :py:mod:`filtering <evutils.filtering>`
     - Event stream filtering.
   * - :py:mod:`io <evutils.io>`
     - Reading and writing events in various file formats.
   * - :py:mod:`jit <evutils.jit>`
     - Lazy numba compilation helper.
   * - :py:mod:`random <evutils.random>`
     - Generation and perturbation of synthetic events.
   * - :py:mod:`torch <evutils.torch>`
     - PyTorch integration.
   * - :py:mod:`transforms <evutils.transforms>`
     - Data augmentation transforms for events.
   * - :py:mod:`types <evutils.types>`
     - Core data types for event streams.
   * - :py:mod:`vis <evutils.vis>`
     - Visualization tools for event camera data.

Attributes
----------

.. autoapisummary::

   evutils.__version__
   evutils.Event_dtype
   evutils.Trigger_dtype


Classes
-------

.. autoapisummary::

   evutils.EventArray
   evutils.TriggerArray


Package Contents
----------------

.. py:data:: __version__

.. py:class:: EventArray(t: numpy.typing.ArrayLike, x: numpy.typing.ArrayLike, y: numpy.typing.ArrayLike, p: numpy.typing.ArrayLike, metadata: dict | None = None)

   Bases: :py:obj:`SoaArray`

   .. autoapi-inheritance-diagram:: evutils.EventArray
      :parts: 1


   A container for storing event data in a struct-of-arrays (SoA) layout.

   The four fields ``t``, ``x``, ``y`` and ``p`` are kept as separate
   contiguous numpy arrays. This is the native layout of the C parser and
   avoids the padding of the packed :data:`Event_dtype` struct.

   Both attribute access (``events.t``) and key access (``events['t']``) return
   the underlying column, so most code written for structured arrays keeps
   working. ``np.asarray(events)`` yields the array-of-structures form (see
   :meth:`__array__`), which lets EventArray flow into code that still expects
   :data:`Event_dtype`.

   .. rubric:: Examples

   >>> from evutils.types import EventArray
   >>> events = EventArray(t=[100, 150], x=[10, 20], y=[30, 40], p=[1, 0])
   >>> events.t
   array([100, 150])
   >>> events.x
   array([10, 20], dtype=uint16)
   >>> events.y
   array([30, 40], dtype=uint16)
   >>> events.p
   array([1, 0], dtype=uint8)
   >>> events[:10]
   EventArray(n=2):
   [(100, 10, 30, 1) (150, 20, 40, 0)]
   >>> events[0]  # doctest: +SKIP
   np.void((100, 10, 30, 1), dtype=[('t', '<i8'), ('x', '<u2'), ('y', '<u2'), ('p', 'u1')])


   .. py:attribute:: __slots__
      :value: ['t', 'x', 'y', 'p']



   .. py:attribute:: t


   .. py:attribute:: x


   .. py:attribute:: y


   .. py:attribute:: p


   .. py:attribute:: metadata
      :value: None


      Optional lightweight, per-array metadata (e.g. ``sensor_size``).

      Defaults to ``None`` and is never part of the AoS/NumPy view. It is
      carried through slicing and :meth:`copy` (shallow), so a sensor size
      stamped on read survives most event processing. Transforms that change
      the meaning of a field (e.g. spatial cropping) are responsible for
      updating it.


.. py:class:: TriggerArray(t: numpy.typing.ArrayLike, p: numpy.typing.ArrayLike, id: numpy.typing.ArrayLike, metadata: dict | None = None)

   Bases: :py:obj:`SoaArray`

   .. autoapi-inheritance-diagram:: evutils.TriggerArray
      :parts: 1


   A container for storing trigger data in a struct-of-arrays (SoA) layout.

   .. rubric:: Examples

   >>> from evutils.types import TriggerArray
   >>> triggers = TriggerArray(t=[1000, 2000], p=[1, 0], id=[0, 1])
   >>> triggers.t
   array([1000, 2000])
   >>> triggers.p
   array([1, 0], dtype=uint8)
   >>> triggers.id
   array([0, 1], dtype=uint8)


   .. py:attribute:: __slots__
      :value: ['t', 'p', 'id']



   .. py:attribute:: t


   .. py:attribute:: p


   .. py:attribute:: id


   .. py:attribute:: metadata
      :value: None


      Optional lightweight, per-array metadata (e.g. ``sensor_size``).

      Defaults to ``None`` and is never part of the AoS/NumPy view. It is
      carried through slicing and :meth:`copy` (shallow), so a sensor size
      stamped on read survives most event processing. Transforms that change
      the meaning of a field (e.g. spatial cropping) are responsible for
      updating it.


.. py:data:: Event_dtype

.. py:data:: Trigger_dtype

